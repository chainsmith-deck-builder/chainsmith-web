/**
 * Placeholder generated client type. Replaced by `pnpm api:generate` against
 * the chainsmith openapi.json at the SHA pinned in .api-version.
 *
 * See .claude/rules/api-client.md for the contract this file fulfills.
 */

export type paths = Record<string, never>;
export type components = { schemas: Record<string, never> };
export type operations = Record<string, never>;
